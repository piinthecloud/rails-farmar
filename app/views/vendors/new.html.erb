<h1> New Vendor </h1>

<%= form_for @vendor do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :market_id %>
  <%= f.text_field :market_id %>

  <%= f.label :location %>
  <%= f.text_field :location %>

  <%= f.submit "Create" %>

<% end %>
